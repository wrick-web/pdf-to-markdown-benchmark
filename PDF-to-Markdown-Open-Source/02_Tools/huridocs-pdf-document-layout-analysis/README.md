# huridocs/pdf-document-layout-analysis

Apache-2.0 · Python (Docker-only distribution) · https://github.com/huridocs/pdf-document-layout-analysis

**Status: prepared-not-run.** This sandbox has the `docker` CLI but no
running daemon, so this Docker-only service could not be started at all —
an infrastructure blocker, independent of the network-egress blockers
that stopped PaddleOCR-VL. See `observations.md` for the exact evidence
(`logs/docker_daemon_check.log`) and `setup/RUN.md` for the full
docker-run/curl command sequence to use on a machine with Docker.
