Not used this cycle — no tool tested here needed a separate installation
script beyond `pip install`/`apt install` (see 02_Tools/<tool>/setup/),
a dedicated extraction-only step (extraction was inline in each
conversion script), or a separate evaluation script (scoring was manual
against the rubric in 00_Project_Notes/Methodology.md, backed by the
quantitative fields each conversion script already saves). Kept as part of
the standard project structure for future cycles / heavier tools (e.g. a
Docker-based install script for huridocs, or a dedicated table-TEDS
evaluation script if ground-truth table annotations are built later).
